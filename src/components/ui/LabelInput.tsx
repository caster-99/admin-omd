export const LabelInput = ({ children }: { children: React.ReactNode }) => {
    return (
        <div className="flex gap-2 items-start flex-col w-full">
            {children}
        </div>
    )
}